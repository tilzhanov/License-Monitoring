---
phase: 03.1-ui-polish
plan: 04
status: complete
completed: 2026-04-16
---

# Plan 03.1-04 Summary — Visual UAT & Phase Gate

## What Was Built

Task 1 — Consolidated UI-16/UI-17/UI-18 smoke assertions added to `tests/test_ui_polish.py`:
- `test_ui16_htmx_request_indicators` — asserts fadeIn keyframe, htmx-request opacity/pointer-events on `#license-tbody` and `#stats-section`
- `test_ui17_skip_link_styled` — asserts `.skip-link` CSS with off-screen top and `:focus` reveal
- `test_ui17_universal_focus_visible` — asserts `:focus-visible` global rule, accent-600 outline, shadow-focus
- `test_ui17_reduced_motion_disables_transitions` — asserts `@media (prefers-reduced-motion)` with near-zero durations
- `test_ui18_requirement_coverage` — scans `.planning/REQUIREMENTS.md` for all UI-01..UI-18 IDs (skipped in Docker where `.planning/` is not copied)
- `test_ui18_all_templates_free_of_inline_styles` — zero `style=` across all `templates/**/*.html`
- `test_ui18_htmx_attr_preservation` — confirms every HTMX attribute from RESEARCH §Test Contract Inventory is still present in `index.html` and `license_table.html`

Also fixed: `test_ui18_requirement_coverage` wrapped in `@pytest.mark.skipif(not REQS.exists())` to avoid Docker false failures since `.planning/` is not deployed to the container.

Task 2 — `03.1-VALIDATION.md` filled:
- `nyquist_compliant: true`, `wave_0_complete: true` set in frontmatter
- Per-Task Verification Map filled for all 9 task rows (03.1-01-01 → 03.1-04-03)
- Validation Sign-Off checkboxes all checked

Task 3 — Visual UAT (human checkpoint):
- App running at `http://localhost:8080/`
- 18-step checklist from UI-SPEC §Delivery Contract item 7 presented to user
- **User approved on 2026-04-16**

## Test Results

| Suite | Local | Docker |
|-------|-------|--------|
| `tests/test_ui_polish.py` | 40 tests — **100% pass** | 39 pass, 1 skip (REQS not in image) |
| `tests/` (full) | **102 passed** | 98 passed, 1 skipped, 3 pre-existing test_config.py failures |

The 3 `test_config.py` Docker failures are pre-existing infrastructure issues (`.env.example` and `.gitignore` not COPY'd into the Docker image) — not regressions introduced by Phase 03.1.

## Deferred Follow-ups

- **Font CDN block**: If internal network blocks `fonts.gstatic.com`, self-host Inter + JetBrains Mono via RESEARCH §Font Loading Strategy escape hatch. No action needed until reported.
- **test_config.py Docker failures**: Root cause is `Dockerfile` not copying `.env.example` / `.gitignore`. Fix belongs in a dedicated infra phase (not UI polish scope).

## Phase 03.1 Close Confirmation

All success criteria met:
- [x] All UI-01..UI-18 requirements covered by automated + manual checks
- [x] All Phase 2/3 tests stay green (102 locally, 98+skip in Docker)
- [x] Visual dashboard matches UI-SPEC palette/spacing/typography/hierarchy
- [x] HTMX swaps polished (fade + fadeIn) without breaking functionality
- [x] A11y gate passes (skip-link, focus-visible, keyboard nav, reduced motion)
- [x] Phase 03.1 UAT approved by user

**Phase 03.1 is ready to close.**
