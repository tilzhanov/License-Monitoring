---
phase: 03.1
slug: ui-polish
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-09
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing, Phase 2/3) |
| **Config file** | `pyproject.toml` / `pytest.ini` (existing) |
| **Quick run command** | `pytest tests/test_ui_polish.py -q` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~10–20 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_ui_polish.py -q`
- **After every plan wave:** Run `pytest tests/ -q` (must stay green — no regressions in Phase 2/3 tests)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

*To be filled by gsd-planner. Each task must map to a REQ-ID (UI-01..UI-18) and an automated check.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | UI-XX | grep/file | `pytest tests/test_ui_polish.py::test_design_tokens -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_ui_polish.py` — new smoke test module with grep-style checks for:
  - Design token presence in CSS (`--color-`, `--space-`, `--font-`)
  - Font `<link>` order in `base.html`
  - Skip link (`<a class="skip-link">`) in `base.html`
  - `:focus-visible` styles in CSS
  - `role="button"` on sortable `<th>` elements
  - SVG sprite `<symbol>` include in `base.html`
  - Color-coded status classes (`.status-active`, `.status-warning`, `.status-expired`) in CSS
- [ ] Existing `tests/conftest.py` reused (no new fixtures expected)
- [ ] Existing pytest install — no framework setup needed

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual parity with UI-SPEC mockups | UI-01..UI-18 | Subjective visual fidelity cannot be grep-checked | Open `http://localhost:8080/`, compare dashboard/detail/forms/404/empty-state against UI-SPEC.md mockups. Verify Linear/Grafana/Vercel aesthetic. |
| HTMX swap polish (no flash, smooth row updates) | UI-XX (HTMX polish) | Interaction/animation quality requires browser | Trigger sort/filter on dashboard, confirm rows update without layout jump or FOUC. |
| Font rendering quality | UI-XX (typography) | Font loading behavior is browser-dependent | Hard-refresh, verify Inter / JetBrains Mono load without FOUT/FOIT on slow network (DevTools throttling). |
| Keyboard nav + focus ring | UI-XX (a11y) | Focus order and visual ring need human eye | Tab through dashboard → detail → form, verify logical order and visible `:focus-visible` ring. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/test_ui_polish.py`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s
- [ ] `nyquist_compliant: true` set in frontmatter after planner fills task map

**Approval:** pending
