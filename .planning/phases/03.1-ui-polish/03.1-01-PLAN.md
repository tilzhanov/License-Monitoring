---
phase: 03.1-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - static/css/app.css
  - templates/base.html
  - templates/partials/icons.html
  - templates/partials/_macros.html
  - tests/test_ui_polish.py
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-17, UI-18]
must_haves:
  truths:
    - "static/css/app.css declares the full UI-SPEC token set on :root"
    - "Inter + JetBrains Mono load via Google Fonts CDN before app.css in base.html"
    - "base.html has a skip-link, top nav with layout-dashboard icon, and <main id=\"main\">"
    - "templates/partials/icons.html defines a <symbol> sprite with all 12 Lucide icons and is included once in base.html"
    - "templates/partials/_macros.html exports the status_badge(status) macro"
    - "tests/test_ui_polish.py smoke-asserts token / font / skip-link / sprite / focus-visible / status-class presence"
    - "Full pytest suite stays green after Wave 1"
  artifacts:
    - path: "static/css/app.css"
      provides: "Token-driven design system foundation (palette, spacing, typography, radius, shadow, reset, nav, skip-link, focus-visible, reduced-motion, status rows, buttons, forms, tables, badges — ~500 lines)"
      contains: "--slate-900"
    - path: "templates/base.html"
      provides: "Font links, sprite include, skip-link, redesigned nav, <main id=\"main\">"
      contains: "skip-link"
    - path: "templates/partials/icons.html"
      provides: "SVG symbol sprite for 12 Lucide icons"
      contains: "icon-alert-triangle"
    - path: "templates/partials/_macros.html"
      provides: "status_badge Jinja macro"
      contains: "status_badge"
    - path: "tests/test_ui_polish.py"
      provides: "Smoke test module for UI polish (grep-style assertions against CSS + base.html)"
      contains: "def test_design_tokens_present"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UI-01..UI-18 requirement IDs added under new UI Polish section"
      contains: "UI-18"
  key_links:
    - from: "templates/base.html"
      to: "static/css/app.css"
      via: "<link rel=stylesheet>"
      pattern: "css/app.css"
    - from: "templates/base.html"
      to: "fonts.googleapis.com"
      via: "<link> before stylesheet"
      pattern: "fonts.googleapis.com/css2"
    - from: "templates/base.html"
      to: "templates/partials/icons.html"
      via: "{% include %}"
      pattern: "partials/icons.html"
---

<objective>
Establish the design-system foundation for Phase 03.1: rewrite `static/css/app.css` as a single token-driven stylesheet matching the locked UI-SPEC palette/spacing/typography/radius/shadow, wire Google Fonts + skip-link + SVG icon sprite into `base.html`, create the reusable `status_badge` macro, seed `tests/test_ui_polish.py` with smoke assertions that every downstream plan will build on, and append UI-01..UI-18 to REQUIREMENTS.md.

Purpose: Every later plan (dashboard, forms/detail, HTMX polish) consumes the tokens declared here. Nothing else can land until this lands.
Output: Rewritten CSS, updated base.html, new partials (icons sprite, macros), new smoke test file, expanded REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-ui-polish/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-polish/03.1-RESEARCH.md
@.planning/phases/03.1-ui-polish/03.1-VALIDATION.md
@CLAUDE.md
@templates/base.html
@static/css/app.css
@tests/test_health.py
@tests/test_dashboard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append UI-01..UI-18 to REQUIREMENTS.md and scaffold tests/test_ui_polish.py smoke suite</name>
  <files>.planning/REQUIREMENTS.md, tests/test_ui_polish.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current v1 section structure; see lines 6–46 for heading pattern)
    - .planning/phases/03.1-ui-polish/03.1-RESEARCH.md §Phase Requirements (UI-01..UI-18 table)
    - .planning/phases/03.1-ui-polish/03.1-VALIDATION.md §Wave 0 Requirements
    - tests/test_health.py (to mirror TestClient fixture usage)
    - tests/conftest.py (to understand existing fixtures)
  </read_first>
  <behavior>
    - test_ui_polish.py::test_design_tokens_present reads static/css/app.css and asserts every token string is present
    - test_ui_polish.py::test_font_links_in_base reads templates/base.html and asserts preconnect + Google Fonts link exist BEFORE the app.css link
    - test_ui_polish.py::test_skip_link_in_base asserts `class="skip-link"` and `href="#main"` and `<main id="main">` in base.html
    - test_ui_polish.py::test_icon_sprite_included asserts base.html includes partials/icons.html and that icons.html declares all 12 Lucide <symbol ids
    - test_ui_polish.py::test_status_row_classes_in_css asserts .status-active, .status-warning, .status-expired rules present in app.css
    - test_ui_polish.py::test_focus_visible_rules_present asserts `:focus-visible` and `prefers-reduced-motion` media query present in app.css
    - test_ui_polish.py::test_status_badge_macro_exists asserts templates/partials/_macros.html defines `macro status_badge`
    - All tests are pure file-read + substring grep — no TestClient needed, runs in <1s
  </behavior>
  <action>
    STEP A — REQUIREMENTS.md update

    Open `.planning/REQUIREMENTS.md`. After the existing `### Infrastructure` subsection (ends around line 46 with INFRA-04), INSERT a new subsection BEFORE the `## v2 Requirements` heading:

    ```markdown
    ### UI Polish (Phase 03.1)

    - [ ] **UI-01**: Introduce a single token-driven CSS design system (colors, spacing, typography, radius, shadow) declared on `:root` in `static/css/app.css`
    - [ ] **UI-02**: Load Inter + JetBrains Mono via Google Fonts CDN with preconnect and `display=swap`, injected in `templates/base.html` `<head>` BEFORE the stylesheet link
    - [ ] **UI-03**: Redesign top navigation bar: 56px tall, slate-900 background, `layout-dashboard` icon before brand, 14px uppercase letter-spacing 0.02em brand, accent-600 underline on current link
    - [ ] **UI-04**: Redesign stat counter cards: label-first then count, 24px tabular-nums count, variant color on count text only, 16px muted icon next to label, no shadow by default
    - [ ] **UI-05**: Redesign expiring-soon widget: slate-900 h2 with amber `alert-triangle` icon, slate-100 dividers, weight-600 product name, days-badge pill with warning or expired tokens
    - [ ] **UI-06**: Redesign license table: `.table-container` wrapper, slate-50 thead with uppercase 12px labels, 36px row content height, status row background + 4px inset left bar, product name cell as weight-600 link
    - [ ] **UI-07**: Replace `▲▼` Unicode sort indicators with inline Lucide `chevron-up` / `chevron-down` SVG in accent-600 12px, add `role="button"`, `tabindex="0"`, and hover `--slate-100` background to sortable headers
    - [ ] **UI-08**: Redesign filter controls: flex row 8px gap, 36px inputs with slate-200 border, inline `search` icon left 12px in search input, focus ring, primary "+ Добавить лицензию" button with `plus` icon
    - [ ] **UI-09**: Redesign buttons (primary / secondary / destructive / action-btn / action-link / action-edit / action-delete) with exact height/padding/color/hover/focus matrix from UI-SPEC §Component 8 including universal `:focus-visible` outline
    - [ ] **UI-10**: Redesign forms: max-width 640px, form-card with slate-200 border + radius-md + space-5 padding + shadow-sm, 12px weight-600 slate-700 labels, 36px inputs with focus ring, `.field-error` with `alert-triangle` icon, required-label `::after " *"`
    - [ ] **UI-11**: Redesign detail page: max-width 840px, breadcrumb with `arrow-left` icon, detail-card shadow-sm, detail-header flex-between with h1 + status badge, detail-fields grid `200px 1fr` with `8px 24px` gap, detail-comment border-top divider
    - [ ] **UI-12**: Redesign 404 page: centered layout, `x-circle` 48px slate-300 icon, dynamic `<h1>{{ title if title else "Страница не найдена" }}</h1>`, 14px slate-500 body, `.btn-secondary` back link with locked Russian copy
    - [ ] **UI-13**: Implement three empty states per UI-SPEC §Component 11 with locked Russian copy (no-licenses full empty, no-filter-match inline row, expiring-widget empty state)
    - [ ] **UI-14**: Ship reusable `.status-badge` component (inline-flex, 2px 8px padding, radius-lg pill, 12px weight-600, inline SVG icon, three variants), used in table Status column and detail-page header via Jinja macro
    - [ ] **UI-15**: Update copy throughout templates to match the locked verb+noun Russian copy table in UI-SPEC §Copywriting Contract
    - [ ] **UI-16**: Add HTMX visual indicators: `.htmx-request { opacity: 0.6; transition: opacity 120ms }` on `#license-tbody`, `pointer-events: none` during swap, `@keyframes fadeIn` on swapped rows, `#stats-section` opacity fade on `license-changed`
    - [ ] **UI-17**: Accessibility gate: skip-link visually hidden until `:focus`, universal `:focus-visible` accent-600 outline, sortable `<th>` reachable via Tab/Enter/Space, `@media (prefers-reduced-motion: reduce)` disables transitions
    - [ ] **UI-18**: Test preservation gate: all Phase 2 & Phase 3 test assertions (test_dashboard.py, test_licenses.py, test_health.py, test_status.py) MUST still pass after UI retrofit — hard constraint on every other UI-xx requirement
    ```

    Also append to the `## Traceability` table (bottom of file):

    ```markdown
    | UI-01 | Phase 03.1 | Pending |
    | UI-02 | Phase 03.1 | Pending |
    | UI-03 | Phase 03.1 | Pending |
    | UI-04 | Phase 03.1 | Pending |
    | UI-05 | Phase 03.1 | Pending |
    | UI-06 | Phase 03.1 | Pending |
    | UI-07 | Phase 03.1 | Pending |
    | UI-08 | Phase 03.1 | Pending |
    | UI-09 | Phase 03.1 | Pending |
    | UI-10 | Phase 03.1 | Pending |
    | UI-11 | Phase 03.1 | Pending |
    | UI-12 | Phase 03.1 | Pending |
    | UI-13 | Phase 03.1 | Pending |
    | UI-14 | Phase 03.1 | Pending |
    | UI-15 | Phase 03.1 | Pending |
    | UI-16 | Phase 03.1 | Pending |
    | UI-17 | Phase 03.1 | Pending |
    | UI-18 | Phase 03.1 | Pending |
    ```

    STEP B — Create tests/test_ui_polish.py

    Create `tests/test_ui_polish.py` with the following content verbatim (RED initially — all assertions fail until Tasks 2 + 3 complete):

    ```python
    """Phase 03.1 UI Polish smoke tests — grep-style file assertions.

    These tests pin the design-system foundation: token presence in CSS, font
    loading order in base.html, skip-link, icon sprite, focus-visible, and
    status row classes. They run in <1s and must stay green through every
    subsequent 03.1 plan. See .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md
    for locked values.
    """
    from pathlib import Path

    ROOT = Path(__file__).resolve().parent.parent
    CSS = ROOT / "static" / "css" / "app.css"
    BASE = ROOT / "templates" / "base.html"
    ICONS = ROOT / "templates" / "partials" / "icons.html"
    MACROS = ROOT / "templates" / "partials" / "_macros.html"


    def _css() -> str:
        return CSS.read_text(encoding="utf-8")


    def _base() -> str:
        return BASE.read_text(encoding="utf-8")


    def test_design_tokens_present():
        css = _css()
        # Spacing scale (12px MUST be absent from scale, only 4/8/16/24/32/48/64)
        for token in ["--space-1:", "--space-2:", "--space-4:", "--space-5:",
                      "--space-6:", "--space-8:", "--space-10:"]:
            assert token in css, f"missing spacing token {token}"
        # Typography
        for token in ["--font-sans:", "--font-mono:", "--text-xs:", "--text-sm:",
                      "--text-lg:", "--text-xl:", "--fw-regular:", "--fw-semibold:"]:
            assert token in css, f"missing typography token {token}"
        # Slate ramp
        for token in ["--slate-50:", "--slate-100:", "--slate-200:", "--slate-300:",
                      "--slate-500:", "--slate-700:", "--slate-900:", "--surface:"]:
            assert token in css, f"missing slate token {token}"
        # Accent
        assert "--accent-600:" in css
        assert "--accent-700:" in css
        # Status tokens
        for token in ["--status-active-bg:", "--status-active-bar:",
                      "--status-warning-bg:", "--status-warning-bar:",
                      "--status-expired-bg:", "--status-expired-bar:",
                      "--destructive:"]:
            assert token in css, f"missing status token {token}"
        # Radius + shadow
        for token in ["--radius-sm:", "--radius-md:", "--radius-lg:",
                      "--shadow-sm:", "--shadow-focus:"]:
            assert token in css, f"missing radius/shadow token {token}"
        # Locked HEX values from UI-SPEC §Color
        for hex_value in ["#0F172A", "#2563EB", "#F8FAFC", "#22C55E",
                          "#F59E0B", "#EF4444", "#DC2626"]:
            assert hex_value in css, f"missing locked hex {hex_value}"


    def test_font_links_in_base():
        html = _base()
        assert 'rel="preconnect" href="https://fonts.googleapis.com"' in html
        assert 'rel="preconnect" href="https://fonts.gstatic.com"' in html
        assert "fonts.googleapis.com/css2?family=Inter" in html
        assert "JetBrains+Mono" in html
        assert "display=swap" in html
        # Fonts MUST load before app.css
        fonts_idx = html.find("fonts.googleapis.com/css2")
        css_idx = html.find("css/app.css")
        assert fonts_idx != -1 and css_idx != -1
        assert fonts_idx < css_idx, "fonts must be linked BEFORE app.css"


    def test_skip_link_and_main_id_in_base():
        html = _base()
        assert 'class="skip-link"' in html
        assert 'href="#main"' in html
        assert 'id="main"' in html
        assert "Перейти к содержимому" in html


    def test_brand_text_preserved():
        # test_health.py:69 pins this literally
        assert "License Monitor" in _base()


    def test_icon_sprite_included_and_complete():
        html = _base()
        assert 'partials/icons.html' in html
        assert ICONS.exists(), "templates/partials/icons.html must exist"
        sprite = ICONS.read_text(encoding="utf-8")
        for icon_id in [
            "icon-layout-dashboard", "icon-plus", "icon-pencil", "icon-trash-2",
            "icon-chevron-up", "icon-chevron-down", "icon-search",
            "icon-alert-triangle", "icon-check-circle-2", "icon-x-circle",
            "icon-calendar", "icon-arrow-left",
        ]:
            assert f'id="{icon_id}"' in sprite, f"sprite missing {icon_id}"


    def test_status_row_classes_in_css():
        css = _css()
        for selector in ["tr.status-active", "tr.status-warning", "tr.status-expired"]:
            assert selector in css, f"missing row selector {selector}"
        assert "inset 4px 0 0 0" in css, "status rows must use inset left bar box-shadow"


    def test_focus_visible_and_reduced_motion_in_css():
        css = _css()
        assert ":focus-visible" in css
        assert "prefers-reduced-motion" in css
        assert "--shadow-focus" in css


    def test_status_badge_macro_exists():
        assert MACROS.exists(), "templates/partials/_macros.html must exist"
        macros = MACROS.read_text(encoding="utf-8")
        assert "macro status_badge" in macros
        assert "status-badge-active" in macros
        assert "status-badge-warning" in macros
        assert "status-badge-expired" in macros
        assert "Активна" in macros
        assert "Скоро истекает" in macros
        assert "Истекла" in macros


    def test_nav_has_dashboard_icon_and_main_wrapper():
        html = _base()
        # Nav brand must reference layout-dashboard icon via sprite <use>
        assert 'href="#icon-layout-dashboard"' in html
        # main wrapper with id for skip-link target
        assert '<main id="main">' in html
    ```
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('tests/test_ui_polish.py'); assert t.exists() and 'test_design_tokens_present' in t.read_text() and 'test_icon_sprite_included_and_complete' in t.read_text(); r=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); assert 'UI-01' in r and 'UI-18' in r and '### UI Polish' in r"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_ui_polish.py` exists and defines at least 9 test functions
    - `grep -c '^def test_' tests/test_ui_polish.py` returns 9 or more
    - `.planning/REQUIREMENTS.md` contains `### UI Polish (Phase 03.1)` heading
    - `.planning/REQUIREMENTS.md` contains every ID `UI-01` through `UI-18`
    - `.planning/REQUIREMENTS.md` Traceability table contains `| UI-18 | Phase 03.1 |`
    - Existing Phase 2/3 tests are not modified
  </acceptance_criteria>
  <done>Wave 0 test scaffold exists, requirements registered, tests are RED (expected — they pass after Tasks 2+3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite static/css/app.css as the full token-driven design system</name>
  <files>static/css/app.css</files>
  <read_first>
    - .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md (entire file — this is the primary source of truth; specifically §Design System, §Spacing Scale, §Typography, §Color, §Visual Hierarchy, §Component Contracts 1–12, §Interaction Contracts, §Accessibility Contract, §Delivery Contract)
    - .planning/phases/03.1-ui-polish/03.1-RESEARCH.md §CSS Architecture Strategy, §Code Examples (token declaration + table + focus-visible blocks are ready to copy)
    - static/css/app.css (current 189-line file — WILL BE REPLACED wholesale)
    - tests/test_ui_polish.py (the acceptance target created in Task 1)
  </read_first>
  <behavior>
    - After rewrite, tests/test_ui_polish.py::test_design_tokens_present passes (all tokens + locked HEX values present)
    - test_status_row_classes_in_css passes (`tr.status-active`, `tr.status-warning`, `tr.status-expired` with `inset 4px 0 0 0` box-shadow)
    - test_focus_visible_and_reduced_motion_in_css passes (`:focus-visible`, `prefers-reduced-motion`, `--shadow-focus`)
    - Full pytest suite still green (no regressions in Phase 2/3)
    - File is ≤ ~550 lines, no @import, no preprocessor syntax
  </behavior>
  <action>
    REPLACE the entire contents of `static/css/app.css` with a token-driven stylesheet organized top-to-bottom per RESEARCH.md §CSS Architecture Strategy. Copy the EXACT values from UI-SPEC.md — no paraphrasing, no re-pickings.

    ## Section 1: :root tokens (spec §Design System + §Spacing + §Typography + §Color + §Border-radius + §Shadow)

    ```css
    /* License Monitor — Phase 03.1 UI Polish design system.
       Source of truth: .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md
       Style family: flat / data-first / subtle-border (Linear / Grafana / Vercel).
       Do NOT add 12px to the spacing scale. Do NOT add weight 500. */
    :root {
      /* Spacing — strict 8-point (12px removed entirely) */
      --space-1: 4px;  --space-2: 8px;  --space-4: 16px;
      --space-5: 24px; --space-6: 32px; --space-8: 48px; --space-10: 64px;

      /* Typography */
      --font-sans: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      --font-mono: "JetBrains Mono", ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      --text-xs: 12px;  --text-sm: 14px;  --text-lg: 18px;  --text-xl: 24px;
      --lh-tight: 1.25; --lh-heading: 1.3; --lh-label: 1.4; --lh-body: 1.5;
      --fw-regular: 400; --fw-semibold: 600;

      /* Neutral ramp — Slate Ops palette */
      --slate-50:  #F8FAFC;  --slate-100: #F1F5F9; --slate-200: #E2E8F0;
      --slate-300: #CBD5E1;  --slate-500: #64748B; --slate-700: #334155;
      --slate-900: #0F172A;  --surface:   #FFFFFF;

      /* Accent (10% — reserved list only) */
      --accent-600: #2563EB; --accent-700: #1D4ED8;

      /* Semantic status (functional, not accent) */
      --status-active-bg:         #F0FDF4;
      --status-active-bar:        #22C55E;
      --status-active-badge-bg:   #DCFCE7;
      --status-active-badge-text: #15803D;

      --status-warning-bg:         #FFFBEB;
      --status-warning-bar:        #F59E0B;
      --status-warning-badge-bg:   #FEF3C7;
      --status-warning-badge-text: #B45309;

      --status-expired-bg:         #FEF2F2;
      --status-expired-bar:        #EF4444;
      --status-expired-badge-bg:   #FEE2E2;
      --status-expired-badge-text: #B91C1C;

      --destructive:       #DC2626;
      --destructive-hover: #B91C1C;

      /* Radius */
      --radius-sm: 4px; --radius-md: 6px; --radius-lg: 999px;

      /* Shadow */
      --shadow-sm: 0 1px 2px 0 rgba(15, 23, 42, 0.04);
      --shadow-focus: 0 0 0 3px rgba(37, 99, 235, 0.25);
    }
    ```

    ## Section 2: reset + body

    ```css
    *, *::before, *::after { box-sizing: border-box; }
    html, body { margin: 0; padding: 0; }
    body {
      font-family: var(--font-sans);
      font-size: var(--text-sm);
      line-height: var(--lh-body);
      background: var(--slate-50);
      color: var(--slate-700);
      -webkit-font-smoothing: antialiased;
    }
    a { color: inherit; text-decoration: none; transition: color 120ms ease-out; }
    h1, h2, h3, h4 { color: var(--slate-900); margin: 0; line-height: var(--lh-heading); }
    h1 { font-size: var(--text-xl); font-weight: var(--fw-semibold); line-height: var(--lh-tight); }
    h2 { font-size: var(--text-lg); font-weight: var(--fw-semibold); }
    p { margin: 0; }
    .icon { width: 16px; height: 16px; display: inline-block; vertical-align: middle; flex-shrink: 0; }
    ```

    ## Section 3: skip link (spec §Accessibility + RESEARCH.md §Pattern 3)

    ```css
    .skip-link {
      position: absolute; top: -40px; left: 0;
      background: var(--accent-600); color: #fff;
      padding: var(--space-2) var(--space-4);
      text-decoration: none; z-index: 100;
      border-radius: 0 0 var(--radius-sm) 0;
    }
    .skip-link:focus { top: 0; }
    ```

    ## Section 4: top nav (spec §Component 1)

    ```css
    nav {
      height: 56px;
      background: var(--slate-900);
      color: #fff;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 var(--space-6);
      border-bottom: 1px solid var(--slate-900);
    }
    .nav-brand {
      display: inline-flex; align-items: center; gap: var(--space-2);
      color: #fff; font-size: var(--text-sm); font-weight: var(--fw-semibold);
      text-transform: uppercase; letter-spacing: 0.02em;
    }
    .nav-brand .icon { width: 16px; height: 16px; color: #fff; }
    .nav-links { display: flex; gap: var(--space-5); }
    .nav-links a {
      color: #fff; font-size: var(--text-sm); font-weight: var(--fw-regular);
      padding-bottom: 2px; border-bottom: 2px solid transparent;
    }
    .nav-links a:hover { border-bottom-color: rgba(255,255,255,0.4); }
    .nav-links a.current { border-bottom-color: var(--accent-600); }
    ```

    ## Section 5: page container (spec §Component 2)

    ```css
    main {
      max-width: 1280px;
      margin: 0 auto;
      padding: var(--space-6) var(--space-6) var(--space-8);
    }
    main > h1 { margin-bottom: var(--space-5); }
    main > h2 { margin: var(--space-6) 0 var(--space-4); }
    ```

    ## Section 6: stats grid + cards (spec §Component 3)

    ```css
    .stats-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: var(--space-4); margin-bottom: var(--space-6);
    }
    .stat-card {
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
      padding: var(--space-5);
    }
    .stat-card .label {
      display: flex; align-items: center; gap: var(--space-2);
      font-size: var(--text-xs); font-weight: var(--fw-semibold);
      color: var(--slate-500);
      text-transform: uppercase; letter-spacing: 0.04em;
      margin: 0 0 var(--space-2);
    }
    .stat-card .label .icon { width: 16px; height: 16px; color: var(--slate-500); }
    .stat-card .count {
      font-size: var(--text-xl);
      font-weight: var(--fw-semibold);
      line-height: var(--lh-tight);
      font-variant-numeric: tabular-nums;
      color: var(--slate-900);
    }
    .stat-card.warning .count { color: var(--status-warning-badge-text); }
    .stat-card.expired .count { color: var(--status-expired-badge-text); }
    ```

    ## Section 7: expiring widget (spec §Component 4)

    ```css
    .expiring-widget {
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
      padding: var(--space-5);
      box-shadow: var(--shadow-sm);
      margin-bottom: var(--space-6);
    }
    .expiring-widget h2 {
      display: flex; align-items: center; gap: var(--space-2);
      margin-bottom: var(--space-4);
    }
    .expiring-widget h2 .icon { color: var(--status-warning-bar); }
    .expiring-widget ul { list-style: none; padding: 0; margin: 0; }
    .expiring-widget li {
      display: flex; justify-content: space-between; align-items: center;
      padding: var(--space-2) 0;
      border-bottom: 1px solid var(--slate-100);
    }
    .expiring-widget li:last-child { border-bottom: none; }
    .expiring-widget .product-name {
      color: var(--slate-900); font-weight: var(--fw-semibold); font-size: var(--text-sm);
    }
    .expiring-widget .expiry-date { color: var(--slate-500); font-variant-numeric: tabular-nums; }
    .days-badge {
      display: inline-block;
      background: var(--status-warning-badge-bg);
      color: var(--status-warning-badge-text);
      padding: 2px var(--space-2);
      border-radius: var(--radius-lg);
      font-size: var(--text-xs);
      font-weight: var(--fw-semibold);
      font-variant-numeric: tabular-nums;
      margin-left: var(--space-2);
    }
    .days-badge.expired { background: var(--status-expired-badge-bg); color: var(--status-expired-badge-text); }
    .expiring-empty { color: var(--slate-500); font-size: var(--text-sm); }
    ```

    ## Section 8: filter controls (spec §Component 7)

    ```css
    .filter-controls {
      display: flex; flex-wrap: wrap; align-items: center;
      gap: var(--space-2);
      margin-bottom: var(--space-4);
    }
    .filter-search {
      position: relative; flex: 1; min-width: 240px;
    }
    .filter-search .icon {
      position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
      width: 16px; height: 16px; color: var(--slate-500); pointer-events: none;
    }
    .filter-search input {
      width: 100%; height: 36px;
      padding: 0 var(--space-4) 0 36px;
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-sm);
      font-size: var(--text-sm); font-family: inherit; color: var(--slate-900);
      background: var(--surface);
    }
    .filter-controls select {
      height: 36px; padding: 0 var(--space-4);
      border: 1px solid var(--slate-200); border-radius: var(--radius-sm);
      font-size: var(--text-sm); font-family: inherit; color: var(--slate-900);
      background: var(--surface);
    }
    .filter-controls input:focus-visible,
    .filter-controls select:focus-visible {
      outline: 2px solid var(--accent-600); outline-offset: 0;
      border-color: var(--accent-600);
      box-shadow: var(--shadow-focus);
    }
    .filter-controls .btn-primary { margin-left: auto; }
    ```

    ## Section 9: table container + table + rows (spec §Component 5 + §Component 6)

    ```css
    .table-container {
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
      overflow: hidden;
    }
    .license-table { width: 100%; border-collapse: collapse; }
    .license-table thead { background: var(--slate-50); }
    .license-table th {
      padding: var(--space-2) var(--space-4);
      text-align: left;
      font-size: var(--text-xs);
      font-weight: var(--fw-semibold);
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--slate-500);
      border-bottom: 1px solid var(--slate-200);
      white-space: nowrap;
    }
    .license-table th[role="button"] {
      cursor: pointer;
      user-select: none;
    }
    .license-table th[role="button"]:hover { background: var(--slate-100); }
    .license-table th .sort-icon {
      width: 12px; height: 12px; margin-left: var(--space-1);
      color: var(--accent-600); vertical-align: middle;
    }
    .license-table td {
      padding: var(--space-2) var(--space-4);
      font-size: var(--text-sm);
      color: var(--slate-700);
      border-bottom: 1px solid var(--slate-200);
      font-variant-numeric: tabular-nums;
    }
    .license-table tr:last-child td { border-bottom: none; }
    .license-table .product-cell a {
      color: var(--slate-900); font-weight: var(--fw-semibold);
    }
    .license-table .product-cell a:hover { color: var(--accent-600); text-decoration: underline; }

    /* Status rows — color + 4px inset left bar (DASH-03) */
    tr.status-active  { background: var(--status-active-bg);  box-shadow: inset 4px 0 0 0 var(--status-active-bar); }
    tr.status-warning { background: var(--status-warning-bg); box-shadow: inset 4px 0 0 0 var(--status-warning-bar); }
    tr.status-expired { background: var(--status-expired-bg); box-shadow: inset 4px 0 0 0 var(--status-expired-bar); }
    tr.status-active:hover  { background: #DCFCE7; }
    tr.status-warning:hover { background: #FEF3C7; }
    tr.status-expired:hover { background: #FEE2E2; }
    ```

    ## Section 10: status badges (spec §Component 6)

    ```css
    .status-badge {
      display: inline-flex; align-items: center; gap: var(--space-1);
      padding: 2px var(--space-2);
      border-radius: var(--radius-lg);
      font-size: var(--text-xs);
      font-weight: var(--fw-semibold);
      line-height: var(--lh-label);
    }
    .status-badge .icon { width: 12px; height: 12px; }
    .status-badge-active  { background: var(--status-active-badge-bg);  color: var(--status-active-badge-text); }
    .status-badge-warning { background: var(--status-warning-badge-bg); color: var(--status-warning-badge-text); }
    .status-badge-expired { background: var(--status-expired-badge-bg); color: var(--status-expired-badge-text); }
    ```

    ## Section 11: buttons + actions (spec §Component 8)

    ```css
    .btn {
      display: inline-flex; align-items: center; gap: var(--space-2);
      height: 32px; padding: 0 var(--space-4);
      border: none; border-radius: var(--radius-sm);
      font-family: inherit; font-size: var(--text-sm); font-weight: var(--fw-semibold);
      cursor: pointer; text-decoration: none;
      transition: background 120ms ease-out, border-color 120ms ease-out, color 120ms ease-out;
    }
    .btn .icon { width: 14px; height: 14px; }
    .btn-primary   { background: var(--accent-600); color: #fff; }
    .btn-primary:hover { background: var(--accent-700); }
    .btn-secondary { background: var(--surface); color: var(--slate-700); border: 1px solid var(--slate-200); }
    .btn-secondary:hover { background: var(--slate-100); border-color: var(--slate-300); }
    .btn-destructive { background: var(--surface); color: var(--destructive); border: 1px solid var(--status-expired-badge-bg); }
    .btn-destructive:hover { background: var(--status-expired-bg); border-color: var(--destructive); }

    .actions-cell { display: flex; gap: var(--space-2); align-items: center; }
    .action-btn, .action-link {
      display: inline-flex; align-items: center; gap: var(--space-1);
      height: 28px; padding: 0 var(--space-2);
      background: transparent; border: none;
      font-family: inherit; font-size: var(--text-sm); font-weight: var(--fw-semibold);
      cursor: pointer; text-decoration: none;
      border-radius: var(--radius-sm);
      transition: color 120ms ease-out, background 120ms ease-out;
    }
    .action-link, .action-edit { color: var(--slate-700); }
    .action-link:hover, .action-edit:hover { color: var(--accent-600); }
    .action-delete { color: var(--destructive); }
    .action-delete:hover { text-decoration: underline; background: var(--status-expired-bg); }
    ```

    ## Section 12: forms (spec §Component 9)

    ```css
    .form-container { max-width: 640px; margin: 0 auto; }
    .form-card {
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
      padding: var(--space-5);
      box-shadow: var(--shadow-sm);
    }
    .form-container > h1 { margin-bottom: var(--space-5); }
    .form-group { margin-bottom: var(--space-4); }
    .form-group:last-of-type { margin-bottom: 0; }
    .form-group label {
      display: block;
      font-size: var(--text-xs);
      font-weight: var(--fw-semibold);
      color: var(--slate-700);
      margin-bottom: var(--space-2);
    }
    .form-group.required label::after { content: " *"; color: var(--destructive); }
    .form-group input,
    .form-group textarea,
    .form-group select {
      width: 100%;
      height: 36px;
      padding: var(--space-2) var(--space-4);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-sm);
      font-size: var(--text-sm);
      font-family: inherit;
      color: var(--slate-900);
      background: var(--surface);
    }
    .form-group textarea { height: auto; min-height: 80px; padding: var(--space-2) var(--space-4); resize: vertical; }
    .form-group input::placeholder,
    .form-group textarea::placeholder { color: var(--slate-500); }
    .form-group input:focus-visible,
    .form-group textarea:focus-visible,
    .form-group select:focus-visible {
      outline: none; border-color: var(--accent-600); box-shadow: var(--shadow-focus);
    }
    .form-group .input-error { border-color: var(--destructive); }
    .field-error {
      display: flex; align-items: center; gap: var(--space-1);
      color: var(--destructive);
      font-size: var(--text-xs);
      margin-top: var(--space-1);
    }
    .field-error .icon { width: 12px; height: 12px; }
    .form-actions {
      display: flex; gap: var(--space-2);
      margin-top: var(--space-5);
    }
    ```

    ## Section 13: detail page (spec §Component 10)

    ```css
    .detail-container { max-width: 840px; margin: 0 auto; }
    .breadcrumb {
      display: flex; align-items: center; gap: var(--space-1);
      font-size: var(--text-xs); color: var(--slate-500);
      margin-bottom: var(--space-4);
    }
    .breadcrumb .icon { width: 12px; height: 12px; }
    .breadcrumb a { color: var(--slate-500); }
    .breadcrumb a:hover { color: var(--accent-600); }
    .detail-card {
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
      padding: var(--space-5);
      box-shadow: var(--shadow-sm);
    }
    .detail-header {
      display: flex; justify-content: space-between; align-items: center;
      margin-bottom: var(--space-5);
    }
    .detail-header h1 { font-size: var(--text-xl); }
    .detail-fields {
      display: grid; grid-template-columns: 200px 1fr;
      gap: var(--space-2) var(--space-5);
    }
    .detail-label {
      font-size: var(--text-xs); font-weight: var(--fw-semibold);
      color: var(--slate-500); text-transform: uppercase; letter-spacing: 0.04em;
    }
    .detail-value {
      font-size: var(--text-sm); color: var(--slate-900);
      font-variant-numeric: tabular-nums;
    }
    .detail-comment {
      margin-top: var(--space-5);
      padding-top: var(--space-4);
      border-top: 1px solid var(--slate-200);
    }
    .detail-comment p { color: var(--slate-700); }
    .detail-actions {
      margin-top: var(--space-5);
      display: flex; gap: var(--space-2);
    }
    ```

    ## Section 14: empty states (spec §Component 11)

    ```css
    .empty-state {
      display: flex; flex-direction: column; align-items: center;
      padding: var(--space-8) var(--space-6);
      text-align: center;
      background: var(--surface);
      border: 1px solid var(--slate-200);
      border-radius: var(--radius-md);
    }
    .empty-state .icon-large { width: 48px; height: 48px; color: var(--slate-300); margin-bottom: var(--space-4); }
    .empty-state h2 { font-size: var(--text-lg); color: var(--slate-900); margin-bottom: var(--space-2); }
    .empty-state p { font-size: var(--text-sm); color: var(--slate-500); max-width: 380px; margin-bottom: var(--space-4); }
    .empty-row td {
      text-align: center;
      padding: var(--space-8) var(--space-4);
      color: var(--slate-500);
      font-size: var(--text-sm);
    }
    ```

    ## Section 15: 404 page (spec §Component 12)

    ```css
    .not-found {
      display: flex; flex-direction: column; align-items: center;
      padding: var(--space-10) var(--space-6);
      max-width: 480px; margin: 0 auto; text-align: center;
    }
    .not-found .icon-large { width: 48px; height: 48px; color: var(--slate-300); margin-bottom: var(--space-4); }
    .not-found h1 { margin-bottom: var(--space-2); }
    .not-found p { color: var(--slate-500); font-size: var(--text-sm); margin-bottom: var(--space-5); }
    ```

    ## Section 16: HTMX indicators (spec §Interaction Contracts)

    ```css
    #license-tbody.htmx-request {
      opacity: 0.6; pointer-events: none;
      transition: opacity 120ms ease-out;
    }
    #stats-section.htmx-request { opacity: 0.6; transition: opacity 120ms ease-out; }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    #license-tbody tr { animation: fadeIn 120ms ease-out; }
    ```

    ## Section 17: focus-visible universal (spec §Accessibility)

    ```css
    :focus-visible {
      outline: 2px solid var(--accent-600);
      outline-offset: 2px;
      box-shadow: var(--shadow-focus);
    }
    .btn:focus-visible,
    .action-btn:focus-visible,
    .action-link:focus-visible {
      outline: 2px solid var(--accent-600);
      outline-offset: 2px;
      box-shadow: var(--shadow-focus);
    }
    ```

    ## Section 18: reduced motion (spec §Accessibility)

    ```css
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        transition-duration: 0.01ms !important;
        animation-duration: 0.01ms !important;
      }
    }
    ```

    ## Section 19: responsive (spec §Delivery Contract manual QA item 12 — horizontal scroll chosen)

    ```css
    @media (max-width: 768px) {
      .stats-grid { grid-template-columns: 1fr; }
      .table-container { overflow-x: auto; }
      .license-table { min-width: 720px; }
      .filter-controls { flex-direction: column; align-items: stretch; }
      .filter-controls .btn-primary { margin-left: 0; }
      main { padding: var(--space-5) var(--space-4) var(--space-6); }
      nav { padding: 0 var(--space-4); }
    }
    ```

    ## Hard rules for this rewrite

    - No `!important` except inside `@media (prefers-reduced-motion)`
    - Keep every existing class name that's referenced by templates or tests: `.stats-grid`, `.stat-card`, `.stat-card .count`, `.stat-card .label`, `.stat-card.total/.warning/.expired`, `.expiring-widget`, `.license-table`, `.filter-controls`, `.btn`, `.btn-primary`, `.btn-secondary`, `.form-container`, `.form-card`, `.form-group`, `.input-error`, `.field-error`, `.form-actions`, `.actions-cell`, `.action-link`, `.action-btn`, `.action-delete`, `.status-badge`, `.status-badge-active/-warning/-expired`, `.detail-container`, `.detail-card`, `.breadcrumb`, `.detail-header`, `.detail-fields`, `.detail-label`, `.detail-value`, `.detail-comment`, `.detail-actions`, `.not-found`, `.empty-state`, `.status-active`, `.status-warning`, `.status-expired`
    - Add these new classes: `.table-container`, `.skip-link`, `.btn-destructive`, `.action-edit`, `.nav-brand`, `.nav-links`, `.filter-search`, `.icon`, `.sort-icon`, `.icon-large`, `.empty-row`, `.expiring-empty`, `.product-cell`, `.days-badge.expired`
    - DO NOT edit templates in this task — templates come in Task 3 and in Plans 02/03/04
  </action>
  <verify>
    <automated>pytest tests/test_ui_polish.py::test_design_tokens_present tests/test_ui_polish.py::test_status_row_classes_in_css tests/test_ui_polish.py::test_focus_visible_and_reduced_motion_in_css -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `static/css/app.css` contains `--slate-900: #0F172A`
    - `static/css/app.css` contains `--accent-600: #2563EB`
    - `static/css/app.css` contains `tr.status-active` AND `inset 4px 0 0 0`
    - `static/css/app.css` contains `:focus-visible`
    - `static/css/app.css` contains `prefers-reduced-motion`
    - `static/css/app.css` contains `.table-container`
    - `static/css/app.css` does NOT contain the literal string `12px` anywhere in the `--space-` token block (grep `--space-.:.*12px` returns no match)
    - `pytest tests/test_ui_polish.py::test_design_tokens_present -q` exits 0
    - `pytest tests/test_ui_polish.py::test_status_row_classes_in_css -q` exits 0
    - `pytest tests/test_ui_polish.py::test_focus_visible_and_reduced_motion_in_css -q` exits 0
    - `pytest tests/ -q` exits 0 (no Phase 2/3 regressions)
  </acceptance_criteria>
  <done>CSS rewritten with all tokens; token/status/focus smoke tests green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite base.html with fonts + skip-link + sprite + redesigned nav + create sprite and macros partials</name>
  <files>templates/base.html, templates/partials/icons.html, templates/partials/_macros.html</files>
  <read_first>
    - templates/base.html (current 22-line file)
    - .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md §Design System (font loading snippet), §Component 1 (nav contract), §Accessibility Contract (skip link), §Copywriting Contract (nav copy)
    - .planning/phases/03.1-ui-polish/03.1-RESEARCH.md §Font Loading Strategy, §Pattern 1 (Icon sprite), §Pattern 2 (status badge macro), §Pattern 3 (skip link)
    - tests/test_ui_polish.py (Task 1 scaffold — this task makes it green)
    - tests/test_health.py (line 69 pins `"License Monitor"` literal in brand — DO NOT change brand text)
  </read_first>
  <behavior>
    - After this task, all 9+ test_ui_polish.py smoke tests pass
    - test_health.py still passes (brand text "License Monitor" preserved)
    - test_dashboard.py + test_licenses.py still pass (no hx-* attributes touched, `<main>` still wraps content block)
  </behavior>
  <action>
    STEP A — Rewrite `templates/base.html` REPLACING the entire file with:

    ```html
    <!DOCTYPE html>
    <html lang="ru">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>{% block title %}License Monitor{% endblock %}</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
      <script src="https://unpkg.com/htmx.org@2.0.4"></script>
      <link rel="stylesheet" href="{{ url_for('static', path='/css/app.css') }}">
    </head>
    <body>
      <a href="#main" class="skip-link">Перейти к содержимому</a>
      {% include "partials/icons.html" %}
      <nav>
        <a href="/" class="nav-brand">
          <svg class="icon" aria-hidden="true"><use href="#icon-layout-dashboard"/></svg>
          License Monitor
        </a>
        <div class="nav-links">
          <a href="/" class="current">Дашборд</a>
        </div>
      </nav>
      <main id="main">
        {% block content %}{% endblock %}
      </main>
    </body>
    </html>
    ```

    Order is critical: preconnect BEFORE the Google Fonts `<link>`, fonts `<link>` BEFORE `htmx.org` script and BEFORE `app.css`. The `License Monitor` text MUST remain literal (test_health.py:69 assertion). Skip-link is the FIRST focusable element inside `<body>`. Sprite include is second so `<use href="#icon-...">` references resolve for the nav brand icon.

    STEP B — Create `templates/partials/icons.html` containing the Lucide symbol sprite. Copy path data verbatim from lucide.dev — the snippets below are the exact, up-to-date Lucide v0.x stroke-based paths. The wrapper `<svg>` MUST have `style="display:none"` and `aria-hidden="true"` so it doesn't consume layout and is ignored by AT.

    ```html
    {# templates/partials/icons.html — Lucide icon sprite.
       Source: https://lucide.dev (MIT). Copied as static SVG per UI-SPEC §Design System.
       Do NOT add JS. Do NOT fetch at runtime. All 12 icons from the reserved set. #}
    <svg xmlns="http://www.w3.org/2000/svg" style="display:none" aria-hidden="true">
      <symbol id="icon-layout-dashboard" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect width="7" height="9" x="3" y="3" rx="1"/>
        <rect width="7" height="5" x="14" y="3" rx="1"/>
        <rect width="7" height="9" x="14" y="12" rx="1"/>
        <rect width="7" height="5" x="3" y="16" rx="1"/>
      </symbol>
      <symbol id="icon-plus" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M5 12h14"/>
        <path d="M12 5v14"/>
      </symbol>
      <symbol id="icon-pencil" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z"/>
        <path d="m15 5 4 4"/>
      </symbol>
      <symbol id="icon-trash-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M3 6h18"/>
        <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/>
        <path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
        <line x1="10" x2="10" y1="11" y2="17"/>
        <line x1="14" x2="14" y1="11" y2="17"/>
      </symbol>
      <symbol id="icon-chevron-up" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="m18 15-6-6-6 6"/>
      </symbol>
      <symbol id="icon-chevron-down" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="m6 9 6 6 6-6"/>
      </symbol>
      <symbol id="icon-search" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="11" cy="11" r="8"/>
        <path d="m21 21-4.3-4.3"/>
      </symbol>
      <symbol id="icon-alert-triangle" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/>
        <path d="M12 9v4"/>
        <path d="M12 17h.01"/>
      </symbol>
      <symbol id="icon-check-circle-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="10"/>
        <path d="m9 12 2 2 4-4"/>
      </symbol>
      <symbol id="icon-x-circle" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="10"/>
        <path d="m15 9-6 6"/>
        <path d="m9 9 6 6"/>
      </symbol>
      <symbol id="icon-calendar" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M8 2v4"/>
        <path d="M16 2v4"/>
        <rect width="18" height="18" x="3" y="4" rx="2"/>
        <path d="M3 10h18"/>
      </symbol>
      <symbol id="icon-arrow-left" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="m12 19-7-7 7-7"/>
        <path d="M19 12H5"/>
      </symbol>
    </svg>
    ```

    STEP C — Create `templates/partials/_macros.html` with the status_badge macro (leading underscore marks it as non-renderable):

    ```jinja
    {# templates/partials/_macros.html — reusable Jinja macros for Phase 03.1 UI.
       Import via: {% from "partials/_macros.html" import status_badge %} #}

    {% macro status_badge(status) -%}
      {%- if status == "active" -%}
        <span class="status-badge status-badge-active">
          <svg class="icon" aria-hidden="true"><use href="#icon-check-circle-2"/></svg>
          Активна
        </span>
      {%- elif status == "warning" -%}
        <span class="status-badge status-badge-warning">
          <svg class="icon" aria-hidden="true"><use href="#icon-alert-triangle"/></svg>
          Скоро истекает
        </span>
      {%- else -%}
        <span class="status-badge status-badge-expired">
          <svg class="icon" aria-hidden="true"><use href="#icon-x-circle"/></svg>
          Истекла
        </span>
      {%- endif -%}
    {%- endmacro %}
    ```

    RULES:
    - DO NOT touch any other template in this task
    - DO NOT remove or rename `{% block content %}` or `{% block title %}`
    - DO NOT change `htmx.org@2.0.4` CDN URL (test_health.py:75 pins `"htmx.org"`)
    - DO NOT change the `url_for('static', path='/css/app.css')` reference
    - DO NOT change `<html lang="ru">`
    - Brand text MUST remain the literal string `License Monitor` (test_health.py:69)
  </action>
  <verify>
    <automated>pytest tests/test_ui_polish.py -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `templates/base.html` contains `rel="preconnect" href="https://fonts.googleapis.com"`
    - `templates/base.html` contains `fonts.googleapis.com/css2?family=Inter:wght@400;600;700`
    - `templates/base.html` contains `JetBrains+Mono`
    - `templates/base.html` contains `display=swap`
    - `templates/base.html` contains `class="skip-link"` and `href="#main"`
    - `templates/base.html` contains `<main id="main">`
    - `templates/base.html` contains `License Monitor` (literal, preserves test_health.py:69)
    - `templates/base.html` contains `href="#icon-layout-dashboard"`
    - `templates/base.html` contains `{% include "partials/icons.html" %}`
    - Google Fonts link appears before `css/app.css` in base.html (byte offset check)
    - `templates/partials/icons.html` exists and contains all 12 `id="icon-..."` symbols
    - `templates/partials/_macros.html` exists and contains `macro status_badge`
    - `pytest tests/test_ui_polish.py -q` exits 0 (all 9+ tests green)
    - `pytest tests/ -q` exits 0 (no Phase 2/3 regressions)
  </acceptance_criteria>
  <done>Foundation complete: CSS tokens, fonts, skip-link, sprite, nav redesign, macros partial — Wave 1 smoke suite fully green and full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_ui_polish.py -q` exits 0 (all smoke tests green)
- `pytest tests/ -q` exits 0 (no Phase 2/3 regressions)
- `grep -c 'hx-' templates/base.html` returns the same count as before (0 — base.html has no hx-* attributes)
- `grep -rn 'style=' templates/base.html` returns no matches (no inline styles)
- `.planning/REQUIREMENTS.md` has UI-01..UI-18 listed AND in Traceability table
</verification>

<success_criteria>
- Token-driven CSS design system in place on `:root` with every locked HEX value and spacing token
- Google Fonts Inter + JetBrains Mono loading before app.css in base.html
- Skip-link, `<main id="main">`, redesigned nav with layout-dashboard icon present
- SVG icon sprite with all 12 Lucide icons available via `<use href="#icon-X"/>`
- status_badge Jinja macro ready for Plans 02/03 to consume
- tests/test_ui_polish.py smoke suite created and green
- REQUIREMENTS.md updated with UI-01..UI-18
- Full pytest suite green (Phase 2/3 contracts preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish/03.1-01-SUMMARY.md` following the SUMMARY template — focus on: tokens declared, font load order verified, sprite icon inventory, files added, and any deviations from UI-SPEC (expected: none).
</output>
