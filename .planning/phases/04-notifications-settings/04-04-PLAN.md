---
phase: 04-notifications-settings
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/routers/settings.py
  - app/routers/licenses.py
  - templates/settings.html
  - templates/license_form.html
  - tests/test_settings.py
  - tests/test_licenses.py
autonomous: false
requirements:
  - SETT-02
  - LIC-06
  - NOTF-05

must_haves:
  truths:
    - "POST /settings/test-notification sends a test message via Telegram and shows result inline per D-10"
    - "License add and edit forms include a notify_days_before field per D-12"
    - "Empty notify_days_before field falls back to global default per D-13"
    - "Per-license threshold is saved to DB and respected by enrich_licenses"
  artifacts:
    - path: "app/routers/settings.py"
      provides: "POST /settings/test-notification endpoint"
      contains: "test_notification"
    - path: "templates/license_form.html"
      provides: "notify_days_before field on add/edit forms"
      contains: "notify_days_before"
  key_links:
    - from: "app/routers/settings.py"
      to: "app/services/telegram.py"
      via: "send_telegram_message for test notification"
      pattern: "send_telegram_message"
    - from: "app/routers/licenses.py"
      to: "License.notify_days_before"
      via: "Form field saved to model"
      pattern: "notify_days_before"
---

<objective>
Wire the test notification endpoint and add per-license threshold override to license forms.

Purpose: Operators need to verify their Telegram configuration works before relying on automated digests. Per-license threshold allows critical licenses (e.g., production vCenter) to trigger earlier warnings than the global default.

Output: Working test notification button on Settings page, notify_days_before field on license forms, visual UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notifications-settings/04-CONTEXT.md
@.planning/phases/04-notifications-settings/04-RESEARCH.md
@.planning/phases/04-notifications-settings/04-01-SUMMARY.md
@.planning/phases/04-notifications-settings/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From app/routers/settings.py:
```python
router = APIRouter(tags=["settings"])
def get_setting(db: Session, key: str, env_fallback: str = "") -> str: ...
def save_setting(db: Session, key: str, value: str): ...
# GET /settings, POST /settings already implemented
```

<!-- From Plan 02 -->
From app/services/telegram.py:
```python
def send_telegram_message(token: str, chat_id: str, text: str) -> dict:
    # Returns {"ok": True} or {"ok": False, "error": str, "error_code": int}
```

From app/routers/licenses.py:
```python
# create_license: POST /licenses — Form fields: product_name, purchase_date, expiry_date, responsible, cost, comment
# update_license: POST /licenses/{id} — Same form fields
# License model has notify_days_before: Mapped[Optional[int]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test notification endpoint and per-license threshold field</name>
  <files>app/routers/settings.py, app/routers/licenses.py, templates/settings.html, templates/license_form.html</files>
  <read_first>app/routers/settings.py, app/routers/licenses.py, app/services/telegram.py, templates/settings.html, templates/license_form.html, app/models.py, app/services/status.py</read_first>
  <action>
1. Add test notification endpoint to `app/routers/settings.py`:

   `POST /settings/test-notification` (sync def):
   - Read `telegram_bot_token` and `telegram_chat_id` from DB using `get_setting(db, ...)`.
   - If either is empty, return an HTML fragment: `<div class="alert alert-error">Сначала настройте токен бота и Chat ID</div>`.
   - Import `from app.services.telegram import send_telegram_message`.
   - Call `send_telegram_message(token, chat_id, "License Monitor: тестовое уведомление отправлено успешно.")`.
   - If result `ok` is True: return `<div class="alert alert-success">Тестовое уведомление отправлено</div>`.
   - If result `ok` is False: return `<div class="alert alert-error">Ошибка: {result["error"]}</div>`.
   - The endpoint returns raw HTML (not a template), targeted by `hx-target="#test-result"` per D-10.

2. Update `templates/settings.html`:
   - The test notification button and `#test-result` div should already be there from Plan 01. Verify the `hx-post="/settings/test-notification"` attribute, `hx-target="#test-result"`, and `hx-swap="innerHTML"` are correct. If not present, add them.

3. Add `notify_days_before` field to `templates/license_form.html` (per D-12, add to BOTH add and edit forms — same template):
   - Add a new `.form-group` block AFTER the comment field and BEFORE `.form-actions`:
   ```html
   <div class="form-group">
     <label for="notify_days_before">Порог уведомления (дни)</label>
     <input type="number" id="notify_days_before" name="notify_days_before"
            value="{{ form_data.get('notify_days_before', '') if form_data else (license.notify_days_before if license and license.notify_days_before else '') }}"
            min="1"
            placeholder="По умолчанию: глобальный порог">
     <span class="field-hint">Оставьте пустым для использования глобального порога</span>
   </div>
   ```
   - Per D-13: placeholder text "По умолчанию: глобальный порог" and helper text below the field.

4. Update `app/routers/licenses.py` to handle the new form field:
   - In `create_license()`: Add `notify_days_before: str = Form("")` parameter. Parse: `parsed_notify = int(notify_days_before) if notify_days_before.strip() else None`. Set on license: `license_obj.notify_days_before = parsed_notify`. Also add to `form_data` dict in error case: `"notify_days_before": notify_days_before`.
   - In `update_license()`: Same — add Form parameter, parse, set `license_obj.notify_days_before = parsed_notify`. Also add to `form_data` dict.
   - In `edit_license()`: No changes needed — `license` object already has `notify_days_before` attribute.

5. Add `.field-hint` CSS class if not already in `static/css/app.css`. Check first — if missing, add: `.field-hint { font-size: 12px; color: var(--slate-500); margin-top: var(--space-1); }` at the end of the forms section.
  </action>
  <verify>
    <automated>cd /home/tilzhanov/Desktop/antigravity/License-Monitoring && grep -c "test_notification" app/routers/settings.py && grep -c "notify_days_before" templates/license_form.html && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_notification" app/routers/settings.py` returns 1
    - `grep -c "send_telegram_message" app/routers/settings.py` returns at least 1
    - `grep -c "notify_days_before" templates/license_form.html` returns at least 2
    - `grep -c 'placeholder="По умолчанию' templates/license_form.html` returns 1
    - `grep -c "notify_days_before" app/routers/licenses.py` returns at least 2
    - All existing tests pass: `pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Test notification endpoint returns inline HTML result; license forms include per-license threshold field with placeholder; create/update routes save notify_days_before to DB; all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for test notification and per-license threshold</name>
  <files>tests/test_settings.py, tests/test_licenses.py</files>
  <read_first>tests/test_settings.py, tests/test_licenses.py, app/routers/settings.py, app/routers/licenses.py, app/services/telegram.py</read_first>
  <action>
1. Add tests to `tests/test_settings.py`:

   - `test_test_notification_success(client)` — Seed AppSettings with `telegram_bot_token=tok123` and `telegram_chat_id=-100`. Patch `app.services.telegram.send_telegram_message` to return `{"ok": True}`. POST `/settings/test-notification`. Assert 200, response contains "Тестовое уведомление отправлено". Covers SETT-02.

   - `test_test_notification_no_credentials(client)` — Do NOT seed bot_token/chat_id (or seed empty). POST `/settings/test-notification`. Assert 200, response contains "Сначала настройте". Covers SETT-02 error case.

   - `test_test_notification_telegram_error(client)` — Seed valid credentials. Patch `send_telegram_message` to return `{"ok": False, "error": "Неверный токен бота", "error_code": 401}`. POST `/settings/test-notification`. Assert response contains "Ошибка" and "Неверный токен бота".

2. Add tests to `tests/test_licenses.py`:

   - `test_create_license_with_threshold(client)` — POST `/licenses` with all required fields plus `notify_days_before=45`. Then GET the license detail page. Verify the license was created. Query the DB directly to confirm `notify_days_before` is 45. Covers LIC-06.

   - `test_create_license_without_threshold(client)` — POST `/licenses` without `notify_days_before`. Query DB to confirm `notify_days_before` is None (global default). Covers D-13.

   - `test_edit_license_threshold_field_rendered(client)` — Create a license, GET its edit page. Assert response contains `name="notify_days_before"`. Covers LIC-06 UI.

   - `test_update_license_with_threshold(client)` — Create a license, POST update with `notify_days_before=90`. Query DB to confirm the value is 90. Covers LIC-06.

For patching in test_settings.py, use `from unittest.mock import patch`:
```python
with patch("app.routers.settings.send_telegram_message", return_value={"ok": True}):
    response = client.post("/settings/test-notification")
```
  </action>
  <verify>
    <automated>cd /home/tilzhanov/Desktop/antigravity/License-Monitoring && pytest tests/test_settings.py tests/test_licenses.py -v && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_settings.py -v` shows at least 10 tests passing (7 from Plan 01 + 3 new)
    - `pytest tests/test_licenses.py -v` passes with new threshold tests
    - `pytest tests/ -x -q` exits 0 (no regressions)
    - `grep -c "test_test_notification" tests/test_settings.py` returns at least 3
    - `grep -c "threshold" tests/test_licenses.py` returns at least 2
  </acceptance_criteria>
  <done>3+ test notification integration tests and 4+ per-license threshold tests pass; full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional UAT</name>
  <what-built>Complete Phase 4 — Settings page with HTMX form save, test notification button, per-license threshold field on add/edit forms, daily scheduler, Telegram service.</what-built>
  <how-to-verify>
    1. Start the app: `docker-compose up -d && docker-compose logs -f app`
    2. Open http://localhost:8080/ — verify "Настройки" link appears in the nav bar
    3. Click "Настройки" — verify the Settings page renders with fields: Токен бота, Chat ID, Порог уведомлений, Время отправки
    4. Fill in test values and click "Сохранить настройки" — verify the form updates inline (no page reload), success message appears
    5. Refresh the page — verify saved values persist
    6. If you have a Telegram bot token: paste it + chat_id, save, then click "Отправить тестовое уведомление" — verify result appears inline below the button
    7. If no Telegram credentials: click test notification with empty fields — verify error message "Сначала настройте..." appears
    8. Go to "Новая лицензия" form — verify "Порог уведомления (дни)" field exists with placeholder "По умолчанию: глобальный порог"
    9. Create a license WITH threshold (e.g., 90) — edit it and verify the value is shown
    10. Create a license WITHOUT threshold — edit it and verify the field is empty
    11. Run `docker-compose exec app pytest tests/ -v` — verify all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- POST /settings/test-notification sends test message and shows inline result
- License forms include notify_days_before field
- Per-license threshold saved and loaded correctly
- Full test suite green (all phases)
- Visual/functional UAT approved by user
</verification>

<success_criteria>
- Test notification works end-to-end (or shows descriptive error)
- Per-license threshold field present on both add and edit forms
- Empty threshold defaults to global (None in DB)
- All 11 phase requirements verified by tests
- Full regression suite passes
- User approves visual UAT
</success_criteria>

<output>
After completion, create `.planning/phases/04-notifications-settings/04-04-SUMMARY.md`
</output>
