---
phase: 03.1-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - templates/index.html
  - templates/partials/license_table.html
  - tests/test_ui_polish.py
  - tests/test_dashboard.py
autonomous: true
requirements: [UI-04, UI-05, UI-06, UI-07, UI-08, UI-13, UI-14, UI-15, UI-16]
must_haves:
  truths:
    - "Dashboard stats cards render label first, count second, with muted icon next to label"
    - "Expiring Soon widget shows alert-triangle icon, slate-100 dividers, weight-600 product names, pill days-badge (warning or expired variant)"
    - "License table is wrapped in .table-container and uses the redesigned header + row-bar pattern"
    - "Sortable table headers have role=button, tabindex=0, and inline chevron-up/chevron-down SVG instead of ▲▼"
    - "Filter controls use .filter-search with inline search icon and 36px height"
    - "Partial license_table.html renders status column as <span class=\"status-badge status-badge-{variant}\"> with inline icon"
    - "Full empty state (no licenses) uses locked copy «Пока нет лицензий»"
    - "test_dashboard.py assertion on line 103 is updated from 'Лицензии пока не добавлены' to 'Пока нет лицензий' in the same commit"
    - "All HTMX attributes preserved verbatim; full pytest suite stays green"
  artifacts:
    - path: "templates/index.html"
      provides: "Redesigned dashboard page: stats cards, expiring widget, filter bar, wrapped table, empty state"
      contains: "Пока нет лицензий"
    - path: "templates/partials/license_table.html"
      provides: "Row fragment with status_badge macro and product-cell link"
      contains: "status_badge"
  key_links:
    - from: "templates/index.html"
      to: "templates/partials/_macros.html"
      via: "{% from 'partials/_macros.html' import status_badge %}"
      pattern: "import status_badge"
    - from: "templates/partials/license_table.html"
      to: "templates/partials/_macros.html"
      via: "{% from 'partials/_macros.html' import status_badge %}"
      pattern: "import status_badge"
    - from: "templates/index.html"
      to: "/licenses/table"
      via: "hx-get on filters and sortable th (UNCHANGED)"
      pattern: "hx-get=\"/licenses/table"
---

<objective>
Polish the dashboard page (index.html + partials/license_table.html) to match UI-SPEC §Components 3–8, 11, 13–16. Preserve every `hx-*` attribute value, all Jinja context variable names, all class names that tests pin on (`stat-card warning`, `stat-card expired`, `expiring-widget`, `status-active/-warning/-expired`, `stats-section`, `license-tbody`). Replace the browser-default look with a Linear/Grafana data-first layout using the tokens and icon sprite from Plan 01.

Purpose: Dashboard is the operator's daily screen and the phase's primary focal point (stat counters with the expired count leading). Getting this right is the phase's biggest visible win.
Output: Polished index.html + partials/license_table.html, extended test_ui_polish.py, updated test_dashboard.py:103 assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-ui-polish/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-polish/03.1-RESEARCH.md
@.planning/phases/03.1-ui-polish/03.1-01-SUMMARY.md
@CLAUDE.md
@templates/index.html
@templates/partials/license_table.html
@templates/base.html
@templates/partials/icons.html
@templates/partials/_macros.html
@static/css/app.css
@tests/test_dashboard.py
@tests/test_ui_polish.py

<interfaces>
<!-- From Plan 01 (_macros.html) — executor calls these directly -->
From templates/partials/_macros.html:
  {% macro status_badge(status) %}  # status ∈ {"active", "warning", "expired"}

From templates/partials/icons.html (sprite — reference with `<use href="#icon-X"/>`):
  - icon-layout-dashboard, icon-plus, icon-pencil, icon-trash-2
  - icon-chevron-up, icon-chevron-down, icon-search
  - icon-alert-triangle, icon-check-circle-2, icon-x-circle
  - icon-calendar, icon-arrow-left

From app/routers/pages.py (Jinja context for index + /licenses/table — DO NOT RENAME):
  - licenses: list[dict]  # each: {license, status, status_class, days_remaining}
  - item.license.product_name: str
  - item.license.expiry_date: date
  - item.license.responsible: str | None
  - item.status: "active" | "warning" | "expired"
  - item.status_class: "status-active" | "status-warning" | "status-expired"
  - item.days_remaining: int
  - total: int
  - expiring: int
  - expired: int
  - expiring_soon: list[dict]  # same shape as licenses
  - current_sort: str
  - current_order: "asc" | "desc"

HTMX attributes that MUST stay byte-identical (test_dashboard.py lines 230–234 pin these):
  - hx-get="/licenses/table"
  - hx-target="#license-tbody"
  - hx-include (present, various values)
  - hx-trigger="keyup changed delay:300ms"
  - hx-trigger="change"
  - #stats-section hx-get="/" hx-trigger="license-changed from:body" hx-target="#stats-section" hx-select="#stats-section" hx-swap="outerHTML"
  - delete button hx-delete="/licenses/{id}" hx-confirm="Удалить лицензию «{name}»? Это действие необратимо." hx-target="closest tr" hx-swap="outerHTML"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/test_ui_polish.py with dashboard + partial assertions AND update test_dashboard.py:103 empty-state copy</name>
  <files>tests/test_ui_polish.py, tests/test_dashboard.py</files>
  <read_first>
    - tests/test_ui_polish.py (created in Plan 01)
    - tests/test_dashboard.py (note line 103 asserts "Лицензии пока не добавлены" — this is the RESEARCH.md §Test Contract Inventory flagged conflict)
    - .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md §Copywriting Contract (locked copy «Пока нет лицензий»)
    - .planning/phases/03.1-ui-polish/03.1-RESEARCH.md §Test Contract Inventory and §Pitfall 5
    - templates/index.html (current), templates/partials/license_table.html (current)
  </read_first>
  <behavior>
    - New test_ui_polish.py assertions cover: stats-section HTMX attrs preserved, filter hx attrs preserved, stat cards label-first order, expiring widget alert-triangle icon reference, table-container wrapper, sortable th has role=button + tabindex=0 + chevron svg, product-cell link, status_badge macro invocation in partial, empty-state locked copy, macros import in both index.html and license_table.html
    - test_dashboard.py:103 is updated to assert "Пока нет лицензий" instead of "Лицензии пока не добавлены"
  </behavior>
  <action>
    STEP A — Update tests/test_dashboard.py line 103

    Locate the single line (line 103) that asserts `"Лицензии пока не добавлены" in response.text` and REPLACE the string literal with `"Пока нет лицензий"`. Do not change any other assertion in the file. Do not touch any other test file. Example sed-equivalent edit:

    ```python
    # BEFORE
    assert "Лицензии пока не добавлены" in response.text
    # AFTER
    assert "Пока нет лицензий" in response.text
    ```

    This change is mandatory because the UI-SPEC Copywriting Contract locks the full-empty-state heading to «Пока нет лицензий» and the test is a fixture on the old copy. Flagged explicitly in RESEARCH.md §Pitfall 5.

    STEP B — APPEND the following new tests to the END of `tests/test_ui_polish.py` (do not modify existing tests from Plan 01):

    ```python
    INDEX = ROOT / "templates" / "index.html"
    PARTIAL_TABLE = ROOT / "templates" / "partials" / "license_table.html"


    def _index() -> str:
        return INDEX.read_text(encoding="utf-8")


    def _partial_table() -> str:
        return PARTIAL_TABLE.read_text(encoding="utf-8")


    def test_index_preserves_stats_section_htmx():
        html = _index()
        assert 'id="stats-section"' in html
        assert 'hx-get="/"' in html
        assert 'hx-trigger="license-changed from:body"' in html
        assert 'hx-target="#stats-section"' in html
        assert 'hx-select="#stats-section"' in html
        assert 'hx-swap="outerHTML"' in html


    def test_index_preserves_filter_htmx():
        html = _index()
        assert 'hx-get="/licenses/table"' in html
        assert 'hx-target="#license-tbody"' in html
        assert 'hx-trigger="keyup changed delay:300ms"' in html
        assert 'hx-trigger="change"' in html
        assert "hx-include=" in html


    def test_index_stat_cards_label_first():
        # Spec §Component 3 requires label BEFORE count (inverted from current).
        html = _index()
        # Stat cards use explicit class strings that tests pin on.
        assert 'class="stat-card total"' in html
        assert 'class="stat-card warning"' in html
        assert 'class="stat-card expired"' in html
        # For each variant, the label span must appear before the count span.
        for variant in ["total", "warning", "expired"]:
            card_start = html.find(f'class="stat-card {variant}"')
            assert card_start != -1
            # Slice the card subtree (approximate — find closing </div> of the card)
            slice_ = html[card_start:card_start + 500]
            label_idx = slice_.find('class="label"')
            count_idx = slice_.find('class="count"')
            assert label_idx != -1 and count_idx != -1, f"card {variant} missing label/count"
            assert label_idx < count_idx, f"card {variant} must render label BEFORE count"


    def test_index_expiring_widget_has_alert_icon():
        html = _index()
        assert 'class="expiring-widget"' in html
        # Header should include the alert-triangle icon via sprite
        assert 'href="#icon-alert-triangle"' in html


    def test_index_table_container_wrapper():
        html = _index()
        assert 'class="table-container"' in html
        assert 'class="license-table"' in html


    def test_index_sortable_th_keyboard_and_chevrons():
        html = _index()
        # Sortable headers must be keyboard-reachable
        assert 'role="button"' in html
        assert 'tabindex="0"' in html
        # Chevrons via sprite (replaces ▲▼)
        assert 'href="#icon-chevron-up"' in html
        assert 'href="#icon-chevron-down"' in html
        # Old Unicode arrows are gone
        assert "&#9650;" not in html
        assert "&#9660;" not in html


    def test_index_filter_search_icon_and_plus_button():
        html = _index()
        assert 'class="filter-search"' in html
        assert 'href="#icon-search"' in html
        assert 'href="#icon-plus"' in html
        # Primary add button must contain the locked copy
        assert "Добавить лицензию" in html


    def test_index_empty_state_locked_copy():
        html = _index()
        assert "Пока нет лицензий" in html
        assert "Добавьте первую лицензию" in html


    def test_partial_table_uses_status_badge_macro():
        html = _partial_table()
        assert "import status_badge" in html
        assert "status_badge(item.status)" in html
        # Plain status Russian text blocks should be gone — only macro handles it now
        # (the macro output still includes the strings but they come from _macros.html)


    def test_partial_table_preserves_delete_htmx():
        html = _partial_table()
        assert 'hx-delete="/licenses/' in html
        assert 'hx-confirm="Удалить лицензию' in html
        assert 'hx-target="closest tr"' in html
        assert 'hx-swap="outerHTML"' in html


    def test_partial_table_preserves_status_row_classes():
        html = _partial_table()
        # Rows still tagged with item.status_class so CSS rules match
        assert "{{ item.status_class }}" in html


    def test_index_no_inline_styles():
        html = _index()
        assert "style=" not in html, "inline style attributes must be removed from index.html"


    def test_index_imports_macros():
        html = _index()
        assert "from \"partials/_macros.html\" import status_badge" in html or \
               "from 'partials/_macros.html' import status_badge" in html
    ```
  </action>
  <verify>
    <automated>python -c "import re,pathlib; t=pathlib.Path('tests/test_dashboard.py').read_text(); assert 'Пока нет лицензий' in t and 'Лицензии пока не добавлены' not in t; u=pathlib.Path('tests/test_ui_polish.py').read_text(); assert u.count('def test_') >= 20 and 'test_partial_table_uses_status_badge_macro' in u"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains the string `"Пока нет лицензий"` (new literal)
    - `tests/test_dashboard.py` does NOT contain `"Лицензии пока не добавлены"` anywhere
    - `tests/test_ui_polish.py` defines at least 12 new test functions (total ≥ 20)
    - `grep -c '^def test_' tests/test_ui_polish.py` returns 20 or more
    - `tests/test_ui_polish.py` references `test_partial_table_uses_status_badge_macro`
    - `tests/test_ui_polish.py` is the ONLY test file that asserts on `table-container`, `role="button"`, `filter-search`
    - No other test file is modified
  </acceptance_criteria>
  <done>Test scaffold is RED for the dashboard assertions (expected — Task 2 makes them green) and test_dashboard.py:103 fixture is updated to match the new locked copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite templates/index.html and templates/partials/license_table.html to match UI-SPEC</name>
  <files>templates/index.html, templates/partials/license_table.html</files>
  <read_first>
    - templates/index.html (current 100-line file — structure will be preserved, visuals restyled)
    - templates/partials/license_table.html (current 21-line file)
    - .planning/phases/03.1-ui-polish/03.1-UI-SPEC.md §Component 3 (stat cards), §Component 4 (expiring widget), §Component 5 (license table), §Component 6 (status badge), §Component 7 (filter controls), §Component 11 (empty states), §Copywriting Contract
    - .planning/phases/03.1-ui-polish/03.1-RESEARCH.md §Pitfall 1 (HTMX attrs MUST NOT be touched), §Pitfall 6 (inline style removal), §Test Contract Inventory (pinned class names), §Pattern 2 (status_badge macro)
    - templates/partials/_macros.html (from Plan 01)
    - templates/partials/icons.html (from Plan 01 — sprite IDs)
    - tests/test_ui_polish.py (Task 1 — acceptance target)
    - tests/test_dashboard.py (lines 103, 112, 115, 116, 123, 124, 132–134, 154, 202, 213, 230–234 — all must still pass)
  </read_first>
  <behavior>
    - All 20+ tests in tests/test_ui_polish.py pass after this task
    - tests/test_dashboard.py (including the updated line 103) all pass
    - tests/test_licenses.py, test_health.py, test_status.py, test_models.py, test_config.py all pass
  </behavior>
  <action>
    STEP A — Rewrite `templates/index.html`. REPLACE the entire file with:

    ```jinja
    {% extends "base.html" %}
    {% from "partials/_macros.html" import status_badge %}

    {% block title %}License Monitor — Дашборд{% endblock %}

    {% block content %}
    <h1>Мониторинг лицензий</h1>

    {# Stats cards — label first, then count (spec §Component 3). Class strings
       "stat-card total", "stat-card warning", "stat-card expired" are pinned by
       tests/test_dashboard.py lines 115–116 — do not rename. #}
    <div id="stats-section" class="stats-grid"
         hx-get="/" hx-trigger="license-changed from:body" hx-target="#stats-section" hx-select="#stats-section" hx-swap="outerHTML">
      <div class="stat-card total">
        <div class="label">
          <svg class="icon" aria-hidden="true"><use href="#icon-layout-dashboard"/></svg>
          Всего лицензий
        </div>
        <div class="count">{{ total }}</div>
      </div>
      <div class="stat-card warning">
        <div class="label">
          <svg class="icon" aria-hidden="true"><use href="#icon-alert-triangle"/></svg>
          Скоро истекает
        </div>
        <div class="count">{{ expiring }}</div>
      </div>
      <div class="stat-card expired">
        <div class="label">
          <svg class="icon" aria-hidden="true"><use href="#icon-x-circle"/></svg>
          Истекло
        </div>
        <div class="count">{{ expired }}</div>
      </div>
    </div>

    {# Expiring soon widget — class "expiring-widget" pinned by test_dashboard.py:124 #}
    <section class="expiring-widget">
      <h2>
        <svg class="icon" aria-hidden="true"><use href="#icon-alert-triangle"/></svg>
        Скоро истекающие
      </h2>
      {% if expiring_soon %}
      <ul>
        {% for item in expiring_soon %}
        <li>
          <span class="product-name">{{ item.license.product_name }}</span>
          <span>
            <span class="expiry-date">{{ item.license.expiry_date.strftime('%d.%m.%Y') }}</span>
            <span class="days-badge{% if item.days_remaining <= 0 %} expired{% endif %}">{{ item.days_remaining }} дн.</span>
          </span>
        </li>
        {% endfor %}
      </ul>
      {% else %}
      <p class="expiring-empty">Нет лицензий, требующих внимания</p>
      {% endif %}
    </section>

    <h2>Все лицензии</h2>

    {% if licenses %}
    <div class="filter-controls">
      <div class="filter-search">
        <svg class="icon" aria-hidden="true"><use href="#icon-search"/></svg>
        <input type="text" name="product"
               hx-get="/licenses/table"
               hx-trigger="keyup changed delay:300ms"
               hx-target="#license-tbody"
               hx-include="[name='status'],[name='sort'],[name='order']"
               placeholder="Поиск по продукту…">
      </div>
      <select name="status"
              hx-get="/licenses/table"
              hx-trigger="change"
              hx-target="#license-tbody"
              hx-include="[name='product'],[name='sort'],[name='order']">
        <option value="">Все статусы</option>
        <option value="active">Активна</option>
        <option value="warning">Скоро истекает</option>
        <option value="expired">Истекла</option>
      </select>
      <input type="hidden" name="sort" value="{{ current_sort }}">
      <input type="hidden" name="order" value="{{ current_order }}">
      <a href="/licenses/new" class="btn btn-primary">
        <svg class="icon" aria-hidden="true"><use href="#icon-plus"/></svg>
        Добавить лицензию
      </a>
    </div>

    <div class="table-container">
      <table class="license-table">
        <thead>
          <tr>
            <th role="button" tabindex="0"
                hx-get="/licenses/table?sort=product_name&order={% if current_sort == 'product_name' and current_order == 'asc' %}desc{% else %}asc{% endif %}"
                hx-target="#license-tbody"
                hx-include="[name='product'],[name='status']">
              Продукт
              {% if current_sort == 'product_name' %}
                {% if current_order == 'asc' %}
                  <svg class="icon sort-icon" aria-hidden="true"><use href="#icon-chevron-up"/></svg>
                {% else %}
                  <svg class="icon sort-icon" aria-hidden="true"><use href="#icon-chevron-down"/></svg>
                {% endif %}
              {% endif %}
            </th>
            <th role="button" tabindex="0"
                hx-get="/licenses/table?sort=expiry_date&order={% if current_sort == 'expiry_date' and current_order == 'asc' %}desc{% else %}asc{% endif %}"
                hx-target="#license-tbody"
                hx-include="[name='product'],[name='status']">
              Дата истечения
              {% if current_sort == 'expiry_date' %}
                {% if current_order == 'asc' %}
                  <svg class="icon sort-icon" aria-hidden="true"><use href="#icon-chevron-up"/></svg>
                {% else %}
                  <svg class="icon sort-icon" aria-hidden="true"><use href="#icon-chevron-down"/></svg>
                {% endif %}
              {% endif %}
            </th>
            <th>Осталось дней</th>
            <th>Статус</th>
            <th>Ответственный</th>
            <th>Действия</th>
          </tr>
        </thead>
        <tbody id="license-tbody">
          {% include "partials/license_table.html" %}
        </tbody>
      </table>
    </div>
    {% else %}
    <div class="empty-state">
      <svg class="icon icon-large" aria-hidden="true"><use href="#icon-layout-dashboard"/></svg>
      <h2>Пока нет лицензий</h2>
      <p>Добавьте первую лицензию, чтобы начать отслеживать сроки их действия.</p>
      <a href="/licenses/new" class="btn btn-primary">
        <svg class="icon" aria-hidden="true"><use href="#icon-plus"/></svg>
        Добавить лицензию
      </a>
    </div>
    {% endif %}
    {% endblock %}
    ```

    CRITICAL preservation rules (RESEARCH.md §Pitfall 1 + §Test Contract Inventory):
    - Class strings `stat-card total`, `stat-card warning`, `stat-card expired`, `expiring-widget`, `license-table` MUST appear verbatim (pinned by test_dashboard.py:115, 116, 124)
    - IDs `stats-section`, `license-tbody` MUST appear verbatim
    - Every `hx-*` attribute value on `#stats-section` (`hx-get`, `hx-trigger`, `hx-target`, `hx-select`, `hx-swap`) MUST be byte-identical to current
    - Filter `hx-get="/licenses/table"`, `hx-target="#license-tbody"`, `hx-trigger="keyup changed delay:300ms"`, `hx-trigger="change"`, `hx-include="..."` MUST be byte-identical
    - No inline `style="..."` attributes anywhere (grep must return 0)
    - The `filter-search` wrapper `<div>` sits INSIDE `.filter-controls`. The `<input name="product">` stays inside it. This is the only structural change to the filter block — hx-* attrs on the input are unchanged.

    STEP B — Rewrite `templates/partials/license_table.html`. REPLACE the entire file with:

    ```jinja
    {% from "partials/_macros.html" import status_badge %}
    {% for item in licenses %}
    <tr class="{{ item.status_class }}">
      <td class="product-cell"><a href="/licenses/{{ item.license.id }}">{{ item.license.product_name }}</a></td>
      <td>{{ item.license.expiry_date.strftime('%d.%m.%Y') }}</td>
      <td>{{ item.days_remaining }}</td>
      <td>{{ status_badge(item.status) }}</td>
      <td>{{ item.license.responsible or '\u2014' }}</td>
      <td class="actions-cell">
        <a href="/licenses/{{ item.license.id }}/edit" class="action-link action-edit">
          <svg class="icon" aria-hidden="true"><use href="#icon-pencil"/></svg>
          Изменить
        </a>
        <button hx-delete="/licenses/{{ item.license.id }}"
                hx-confirm="Удалить лицензию «{{ item.license.product_name }}»? Это действие необратимо."
                hx-target="closest tr"
                hx-swap="outerHTML"
                class="action-btn action-delete">
          <svg class="icon" aria-hidden="true"><use href="#icon-trash-2"/></svg>
          Удалить
        </button>
      </td>
    </tr>
    {% endfor %}
    ```

    Preservation rules for the partial:
    - `class="{{ item.status_class }}"` on `<tr>` — pinned by test_dashboard.py:132–134
    - `hx-delete`, `hx-confirm`, `hx-target="closest tr"`, `hx-swap="outerHTML"` on delete button — byte-identical
    - The Russian status label is now emitted ONLY via the `status_badge` macro (which still outputs "Активна" / "Скоро истекает" / "Истекла" — preserving any implicit copy contracts)
    - Keep the em-dash fallback `\u2014` for missing responsible
    - DO NOT touch row ordering; tests assert positional ordering on the full response text
  </action>
  <verify>
    <automated>pytest tests/test_ui_polish.py -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `templates/index.html` contains `class="stat-card total"`, `class="stat-card warning"`, `class="stat-card expired"`
    - `templates/index.html` contains `id="stats-section"`, `id="license-tbody"`
    - `templates/index.html` contains `hx-trigger="keyup changed delay:300ms"` and `hx-trigger="change"`
    - `templates/index.html` contains `href="#icon-chevron-up"` AND `href="#icon-chevron-down"`
    - `templates/index.html` does NOT contain `&#9650;` or `&#9660;` (Unicode arrows removed)
    - `templates/index.html` contains `role="button"` and `tabindex="0"`
    - `templates/index.html` contains `Пока нет лицензий`
    - `templates/index.html` contains `class="table-container"`
    - `grep -c 'style=' templates/index.html` returns 0
    - `templates/partials/license_table.html` contains `import status_badge` and `status_badge(item.status)`
    - `templates/partials/license_table.html` contains `hx-delete="/licenses/`, `hx-confirm="Удалить лицензию`, `hx-target="closest tr"`, `hx-swap="outerHTML"`
    - `templates/partials/license_table.html` contains `class="{{ item.status_class }}"`
    - `pytest tests/test_ui_polish.py -q` exits 0
    - `pytest tests/test_dashboard.py -q` exits 0 (all 14+ dashboard tests green including updated :103 assertion)
    - `pytest tests/ -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Dashboard and table partial match UI-SPEC §Components 3–8, 11, 13–16; all HTMX preserved; full pytest suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_ui_polish.py -q` exits 0
- `pytest tests/test_dashboard.py -q` exits 0 (including updated line 103)
- `pytest tests/ -q` exits 0 (no regressions)
- `grep -c 'hx-' templates/index.html` returns the same or higher count than before the rewrite (we preserve every hx attr)
- `grep -c 'style=' templates/index.html` returns 0
</verification>

<success_criteria>
- Dashboard renders stats cards label-first with tabular-nums counts
- Expiring widget shows alert-triangle icon, pill days-badge
- License table wrapped in .table-container, sortable th keyboard-accessible with chevrons
- Filter bar has inline search icon and primary "+ Добавить лицензию" button
- Row fragment uses status_badge macro with inline icon
- Full empty state matches locked copy
- All HTMX attributes byte-identical; Phase 2/3 test contracts preserved
- test_dashboard.py:103 copy updated in same commit
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish/03.1-02-SUMMARY.md` documenting: files modified, HTMX attrs preserved (count), test copy update noted, any deviations (expected: none).
</output>
