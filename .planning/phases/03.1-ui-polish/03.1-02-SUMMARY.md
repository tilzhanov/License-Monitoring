---
phase: 03.1-ui-polish
plan: 02
subsystem: ui
tags: [dashboard, htmx, jinja2, accessibility, icons, status-badge]

requires:
  - phase: 03.1-ui-polish
    plan: 01
    provides: Token-driven CSS, Lucide icon sprite, status_badge macro, test_ui_polish.py foundation

provides:
  - Redesigned dashboard page with label-first stat cards, alert-triangle expiring widget, table-container wrapper
  - Keyboard-accessible sortable headers with SVG chevrons (role=button, tabindex=0)
  - Filter bar with inline search icon and primary add button
  - Row fragment using status_badge macro with inline icons
  - Empty state with locked copy matching UI-SPEC Copywriting Contract
  - 14 new smoke tests pinning dashboard + partial structure

affects: [03.1-04-htmx-polish]

tech-stack:
  added: []
  patterns:
    - SVG sprite icons consumed via <use href="#icon-*"> in dashboard and table partial
    - Jinja status_badge macro replaces inline status text in table rows
    - Sortable table headers use role=button + tabindex=0 for keyboard accessibility

key-files:
  created: []
  modified:
    - templates/index.html
    - templates/partials/license_table.html
    - tests/test_ui_polish.py
    - tests/test_dashboard.py

key-decisions:
  - "Label-first stat card layout (icon + label above count) per UI-SPEC Component 3"
  - "Unicode arrows (&#9650;/&#9660;) replaced with SVG chevron-up/chevron-down from sprite"
  - "Empty state copy locked to 'Пока нет лицензий' per UI-SPEC Copywriting Contract"
  - "test_dashboard.py:103 updated in same commit as test scaffold to maintain copy contract"

requirements-completed: [UI-04, UI-05, UI-06, UI-07, UI-08, UI-13, UI-14, UI-15, UI-16]

duration: 6min
completed: 2026-04-09
---

# Phase 03.1 Plan 02: Dashboard & Table Polish Summary

**Rewrote index.html and license_table.html to match UI-SPEC Components 3-8, 11, 13-16: label-first stat cards with icons, alert-triangle expiring widget, keyboard-accessible sortable headers with SVG chevrons, filter bar with search icon, status_badge macro in row fragment, locked empty-state copy.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-09T10:39:55Z
- **Completed:** 2026-04-09T10:46:22Z
- **Tasks:** 2 (both TDD)
- **Files modified:** 4 (0 created, 4 modified)

## Accomplishments

- Stats cards now render label first (icon + text) then count, matching UI-SPEC Component 3
- Expiring widget header includes alert-triangle SVG icon; days-badge gets expired variant class when days <= 0
- License table wrapped in `.table-container` div for horizontal scroll on narrow viewports
- Sortable `<th>` elements have `role="button"`, `tabindex="0"` for keyboard accessibility
- Unicode arrows (&#9650;/&#9660;) replaced with `chevron-up`/`chevron-down` SVG sprite references
- Filter search input wrapped in `.filter-search` div with inline search icon
- Add button uses plus icon from sprite instead of "+" text prefix
- Empty state uses locked copy "Пока нет лицензий" with dashboard icon and call-to-action
- Table partial now uses `status_badge(item.status)` macro instead of inline Jinja conditionals
- Edit/delete actions in table rows include pencil/trash SVG icons
- All 15 HTMX attributes preserved byte-identical
- Zero inline `style=` attributes in index.html (was 2 before)
- `test_dashboard.py:103` updated from "Лицензии пока не добавлены" to "Пока нет лицензий"
- 14 new smoke tests added to `test_ui_polish.py` (total now 33 including Plan 03 tests)

## Task Commits

1. **Task 1: Extend tests + update empty-state copy** -- `07eff9f` (test)
2. **Task 2: Rewrite index.html and license_table.html** -- `109b827` (feat)

## Files Created/Modified

### Modified
- `templates/index.html` -- Complete rewrite: 100 lines to 121 lines; label-first stat cards, alert-triangle widget, table-container, sortable chevrons, filter-search, empty-state
- `templates/partials/license_table.html` -- Complete rewrite: 21 lines to 22 lines; status_badge macro, product-cell link, pencil/trash icons on actions
- `tests/test_ui_polish.py` -- Appended 14 new test functions (lines 134-247) covering dashboard structure, HTMX preservation, stat card order, chevrons, filter-search, empty state, status_badge, delete HTMX, row classes, inline styles, macros import
- `tests/test_dashboard.py` -- Line 103: updated empty-state assertion from old copy to locked "Пока нет лицензий"

## HTMX Preservation

| Attribute | Location | Preserved |
|-----------|----------|-----------|
| `hx-get="/"` | #stats-section | Yes |
| `hx-trigger="license-changed from:body"` | #stats-section | Yes |
| `hx-target="#stats-section"` | #stats-section | Yes |
| `hx-select="#stats-section"` | #stats-section | Yes |
| `hx-swap="outerHTML"` | #stats-section | Yes |
| `hx-get="/licenses/table"` | product input, status select, th headers | Yes |
| `hx-trigger="keyup changed delay:300ms"` | product input | Yes |
| `hx-trigger="change"` | status select | Yes |
| `hx-target="#license-tbody"` | filters + headers | Yes |
| `hx-include="..."` | filters + headers | Yes |
| `hx-delete="/licenses/{id}"` | delete button | Yes |
| `hx-confirm="Удалить лицензию..."` | delete button | Yes |
| `hx-target="closest tr"` | delete button | Yes |
| `hx-swap="outerHTML"` | delete button | Yes |

Total: 15 hx-* attributes, all byte-identical.

## Decisions Made

- **Label-first card layout:** UI-SPEC Component 3 specifies label above count (inverted from Phase 2 original). Icon placed next to label text for visual anchor.
- **SVG chevrons over Unicode:** &#9650;/&#9660; replaced with sprite-referenced chevron-up/chevron-down for visual consistency and scalability.
- **Copy contract enforcement:** test_dashboard.py:103 updated in Task 1 commit to avoid test breakage window between test update and template rewrite.

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None -- all data sources are wired, no placeholder text or hardcoded empty values.

## Verification Results

- `pytest tests/test_ui_polish.py -k "index or partial_table" -q` -- **13 passed** in 0.02s
- `pytest tests/ --ignore=tests/test_ui_polish.py -q` -- **62 passed** in 0.62s (zero regressions)
- `grep -c 'style=' templates/index.html` -- **0** (no inline styles)
- `grep -c 'hx-' templates/index.html` -- **15** (all HTMX preserved)
- `grep -c '&#9650;\|&#9660;' templates/index.html` -- **0** (Unicode arrows removed)

Note: 7 failures in test_ui_polish.py are from Plan 03.1-03 tests (forms/detail/404) -- expected RED state for that plan's TDD cycle.

## Self-Check: PASSED

All 4 modified files exist on disk; both task commit hashes (`07eff9f`, `109b827`) resolve in git history.

---
*Phase: 03.1-ui-polish*
*Completed: 2026-04-09*
